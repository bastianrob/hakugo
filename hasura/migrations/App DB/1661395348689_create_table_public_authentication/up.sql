CREATE TABLE "public"."authentication" ("id" bigserial NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "expired_at" timestamptz NOT NULL, "credential_id" bigint NOT NULL, "code" character(20) NOT NULL, "used" boolean NOT NULL DEFAULT false, PRIMARY KEY ("id") , FOREIGN KEY ("credential_id") REFERENCES "public"."credential"("id") ON UPDATE restrict ON DELETE restrict);COMMENT ON TABLE "public"."authentication" IS E'contains authentication code used for register verification or magic link';
